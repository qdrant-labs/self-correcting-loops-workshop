# Self-Correcting Agentic Retrieval Loops - workshop dependencies
# Python 3.12. Install:  uv pip install -r requirements.txt
# Loose floors below; the pinned VM-image set is frozen to requirements.lock.txt.

# --- Retrieval + embeddings (all local, ONNX via FastEmbed) ---
qdrant-client>=1.12          # Query API (Prefetch + FusionQuery), named vectors
fastembed>=0.5               # dense (bge-base) + miniCOIL sparse + ColBERT late interaction

# --- LLM routing (Claude via LiteLLM) ---
litellm>=1.55                # routes the agent + STOP autorater behind one interface
anthropic                    # Claude SDK (the agent, via LiteLLM)

# --- Dataset build + notebook metrics ---
datasets>=2.19               # MuSiQue from HuggingFace (dataset build only)
scikit-learn                 # AUC / ROC for the live signal benchmark in the notebook
pandas
numpy

# --- Notebook + visualization ---
jupyter
ipykernel
matplotlib

# --- Utilities ---
python-dotenv
tqdm
